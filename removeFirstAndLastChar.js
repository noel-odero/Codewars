function removeChar(str){

    //   let newArr = str.split("")
    //    newArr.splice(0, 1);
    //   newArr.splice(newArr.length -1 , 1);

    //   return newArray.join()

    //  let firstChar = str[0]
    //  let lastChar = str[str.length]

    //  return
      return str.slice(1, str.length - 1)

    };